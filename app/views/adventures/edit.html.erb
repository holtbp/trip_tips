<%= link_to 'Show', getaway_stop_adventure_path(@getaway, @stop, @adventure) %> |
<%= link_to 'Back', getaway_stop_adventures_path(@getaway, @stop) %>

<h2>Editing Adventure</h2>

<%= render 'form' %>
